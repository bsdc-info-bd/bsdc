# BSDC — Repository tree

> Generated by `npm run docs:tree` from the filesystem and each file's `Purpose :` header.
> Source-available. Re-deployment or rebranding is not permitted.

```
.editorconfig
.env.example
.gitattributes
.gitignore
.nvmrc
LICENSE
NOTICE
PUBLIC_LIMITATIONS.md
assets/
  brand/
    bsdc-icon.svg  — The official BSDC application mark (PWA icon, Android adaptive foreground,
    bsdc-logo-dark.svg
    bsdc-logo-light.svg
    bsdc-logo-mono.svg
    bsdc-logo-stacked.svg
    bsdc-logo.svg  — Primary vector wordmark for Bangladesh Software Development Community.
    bsdc-shield.svg
    bsdc-wordmark.svg
    og-card.svg
  icons/
    reactions/
      reaction-angry.svg
      reaction-care.svg
      reaction-clap.svg
      reaction-fire.svg
      reaction-haha.svg
      reaction-like.svg
      reaction-love.svg
      reaction-mindblown.svg
      reaction-sad.svg
      reaction-wow.svg
    sprite.svg  — Inline <use href="/icons/sprite.svg#id"> symbols for marks and glyphs that must render
  illustrations/
    empty-state.svg
    error-state.svg
    maintenance.svg
    not-found.svg
    offline.svg
    welcome.svg
database.rules.json
docs/
  00-MASTER-BLUEPRINT.md
  02-DATA-MODEL.md
  SELF-AUDIT-1.md  — ` header by `npm run docs:tree`, so it cannot drift.
  SELF-AUDIT-2.md
  SELF-AUDIT-3.md
  SELF-AUDIT-4.md
eslint.config.js  — Flat ESLint configuration enforcing the engineering rules of conduct (PART 01.04)
firebase.json
firestore.indexes.json
firestore.rules
functions/
  .gitignore
  lib/
    claims.js  — Custom-claim authority: the only code allowed to grant or revoke privilege (LAW-03).
    claims.js.map
    env.js  — Typed, fail-fast access to server-only configuration.
    env.js.map
    index.js  — Cloud Function entry points. Everything that must be trusted runs here (LAW-03).
    index.js.map
    passkey.js  — Passkey derivation and constant-time verification (PART 05.06, LAW-06).
    passkey.js.map
    passkey.test.js  — Proves the passkey derivation: salted, peppered, constant-time and never stored.
    passkey.test.js.map
    profile.js  — Account provisioning: user document and username reservation on first sign-in.
    profile.js.map
    rateLimit.js  — Server-side attempt throttling for privileged entry points (PART 05.06).
    rateLimit.js.map
  package-lock.json
  package.json
  src/
    claims.ts  — Custom-claim authority: the only code allowed to grant or revoke privilege (LAW-03).
    env.ts  — Typed, fail-fast access to server-only configuration.
    index.ts  — Cloud Function entry points. Everything that must be trusted runs here (LAW-03).
    passkey.test.ts  — Proves the passkey derivation: salted, peppered, constant-time and never stored.
    passkey.ts  — Passkey derivation and constant-time verification (PART 05.06, LAW-06).
    profile.ts  — Account provisioning: user document and username reservation on first sign-in.
    rateLimit.ts  — Server-side attempt throttling for privileged entry points (PART 05.06).
  tsconfig.json
index.html  — Document shell for the BSDC single-page application.
package-lock.json
package.json
postcss.config.js  — PostCSS pipeline: Tailwind token compilation, nesting-free component CSS, autoprefixer.
prettier.config.js  — Deterministic formatting for TS/TSX/JS/CSS/JSON/MD/HTML/YAML.
public/
  firebase-messaging-sw.js  — The Firebase Cloud Messaging service worker for web push.
  locales/
    bn/
      a11y.json
      about.json
      admin.json
      auth.json
      comments.json
      common.json
      composer.json
      countdown.json
      design-system.json
      errors.json
      events.json
      feed.json
      footer.json
      freelancer.json
      gamification.json
      groups.json
      home.json
      jobs.json
      messenger.json
      moderation.json
      nav.json
      network.json
      notifications.json
      presence.json
      profile.json
      projects.json
      push.json
      reactions.json
      reports.json
      search.json
      stories.json
      theme.json
    en/
      a11y.json
      about.json
      admin.json
      auth.json
      comments.json
      common.json
      composer.json
      countdown.json
      design-system.json
      errors.json
      events.json
      feed.json
      footer.json
      freelancer.json
      gamification.json
      groups.json
      home.json
      jobs.json
      messenger.json
      moderation.json
      nav.json
      network.json
      notifications.json
      presence.json
      profile.json
      projects.json
      push.json
      reactions.json
      reports.json
      search.json
      stories.json
      theme.json
  manifest.webmanifest
  messaging-config.js  — Public Firebase configuration for the messaging service worker.
  opensearch.xml
  robots.txt
  sw.js  — Service worker for the offline experience (PART 16.1, PART 26).
scripts/
  check-no-placeholders.sh  — LAW-02 gate: no placeholder, demo, sample or fake content may ship.
  check-no-workers.sh  — ADR-036 gate: BSDC uses no Cloudflare Worker, at all, ever.
  check-rtdb.mjs  — Static gate over the Realtime Database rules (database.rules.json).
  check-rules.mjs  — Static gate over firestore.rules: every collection the client can reach has a rule,
  generate-repo-tree.mjs  — Regenerates docs/01-REPO-TREE.md from the filesystem and the file headers.
  verify-env.ts  — Environment guard run before every build (PART 06.01).
src/
  app/
    App.tsx  — Composition root: providers, then the router (ADR-006).
    boundaries/
      GlobalErrorBoundary.tsx  — Last-resort boundary around the whole app (PART 24.2).
      OfflineBoundary.tsx  — Offline-capable boundary for data surfaces (PART 16.1, PART 26).
      RouteErrorBoundary.tsx  — Per-route error boundary: one broken screen never blanks the app (PART 24.2).
      SilentBoundary.tsx  — Boundary for optional widgets: a broken widget disappears, the page stays usable
    layouts/
      ErrorLayout.tsx  — Bare layout for error and offline pages (PART 24.2).
      MaintenanceLayout.tsx  — Layout shown during a scheduled maintenance window (PART 03.05).
      PrintLayout.tsx  — Layout used when printing reports, invoices, licences and labels (PART 08.04 R-14).
      PublicLayout.tsx  — Layout for public, indexable pages: header, centred content, footer (PART 07.01).
      RootLayout.tsx  — The application shell: header, main region, footer and bottom navigation
    providers/
      FeatureFlagProvider.tsx  — Publishes flag state to React and re-evaluates scheduled windows (PART 04 LAW-11).
      I18nProvider.tsx  — i18next bootstrap for Bangla and English, both first-class (PART 09.01).
      OfflineProvider.tsx  — Connectivity context that drives the offline banner and the queued-action indicator
      ThemeProvider.tsx  — Applies appearance state to the document element and keeps the browser UI in sync
      ToastProvider.tsx  — The single toast host for the product (PART 08.09, PART 16).
    router/
      routes.tsx  — The application router (PART 07.01, ADR-005).
  core/
    config/
      adPacks.ts  — Ads Engine amount tiers, placements and formats (PART 21.1, PART 21.3).
      app.ts  — Locked product identity, ownership, contact and network facts (PART 03).
      breakpoints.ts  — Single source of truth for the ultra-responsive contract (250px -> 5120px).
      collections.ts  — Every Firestore collection and Realtime Database path in one typed registry.
      features.ts  — The plugin / feature-flag registry seed (PART 04 LAW-11, PART 19.2/17).
      firebase.ts  — Client Firebase options and backend mode, derived from public environment variables.
      flags.ts  — Runtime feature-flag state: schedules, precedence and window validation.
      limits.ts  — Every numeric product limit in one auditable place (PART 29.1, PART 11.03).
      moderation.ts  — The moderation vocabulary: report categories, severities, queue states, SLAs.
      navigation.ts  — The navigation model: which destinations appear in the bottom nav, the rail and the
      notifications.ts  — The vocabulary of notifications: which kinds exist, and the shape of one.
      opportunities.ts  — Shared vocabularies for events, jobs, projects and the freelancer hub.
      permissions.ts  — The single entitlement matrix: who may do what (PART 05.02, LAW-03).
      plans.ts  — Marketplace order-based subscription plans (PART 20.1).
      points.ts  — The reputation economy: point rules, level curve, streak maths and badge catalogue.
      reactions.ts  — The ten BSDC reactions, their order, labels and sprite ids (PART 12.04).
      regions.ts  — Bangladesh administrative reference data used by geo affinity, marketplace
      routes.ts  — The single route table: paths, SEO intent, indexability and build status (PART 07.01).
      schemaTypes.ts  — Registry of the structured-data types BSDC emits (PART 10.07).
      search.ts  — Search scopes, field weights, ranking knobs and the command-palette registry.
    errors/
      AppError.ts  — The single error type carried across the platform (PART 24.4).
      ignore.ts  — The one place a read failure is allowed to end.
      taxonomy.ts  — The single error taxonomy for the whole platform (PART 24.2).
      toUserMessage.ts  — Maps any error to a bilingual, actionable user message (PART 24.2, PART 09.02).
    events/
      bus.ts  — A tiny typed event bus for cross-module notifications that must not create import
    flags/
      flagClient.ts  — Feature-flag state with local override, scheduling and change events (PART 04 LAW-11).
    lib/
      pdf.ts  — Turns a sealed report into a PDF nobody has to take on trust.
      report.ts  — Report identity: the id, the generated-at stamp, the integrity hash and the verify URL.
    logger/
      logger.ts  — Structured logging with redaction and pluggable transports (PART 24.4).
      redact.ts  — Redaction of secrets and personal data before anything is logged (PART 06.01).
    result/
      Result.ts  — Explicit success/failure values for fallible operations (PART 24.4).
  entities/
    admin/
      audit.ts  — The audit trail: what was done, to whom, by whom, and what it looked like before.
      recovery.ts  — The recovery bin: what was soft-deleted, when it disappears, and what can come back.
      repository.ts  — The administrator's reads and writes: flags, roles, the audit trail and the bin.
    comment/
      model.ts  — The comment entity: shape, defaults and threading rules.
      repository.ts  — Comment persistence: paging, create, edit, soft delete and live threads.
    conversation/
      model.ts  — Conversations and chat messages: shape, ids, titles and read state.
      repository.ts  — Messenger persistence: threads, message pages, sends and read receipts.
      thread.ts  — Message reactions, reply threads, attachment validation and delivery state.
    event/
      model.ts  — The event entity: where a community meets, on a map or on a call.
      repository.ts  — Event persistence: publish, browse, answer an invitation, and manage a venue.
    gig/
      model.ts  — The freelancer hub: a gig somebody sells, and an order somebody places for it.
      repository.ts  — Gig and order persistence for the freelancer hub.
    group/
      model.ts  — The group entity: privacy model, membership roles and defaults.
      repository.ts  — Group persistence: listing, creating, joining, leaving and live membership.
      requests.ts  — Join requests, group roles and the visibility rule a secret group relies on.
    job/
      model.ts  — The job entity and the application entity behind it.
      repository.ts  — Job persistence, and the application pipeline behind it.
    media/
      model.ts  — The media asset entity: what we keep about an uploaded file.
    moderation/
      model.ts  — The report and appeal entities, and the vocabulary they are built from.
      repository.ts  — Reporting, the reviewer queue, and appeals.
      state.ts  — The legal transitions of a report, enforced identically in the client and in Functions.
    notification/
      model.ts  — The notification entity: construction, unread maths, ordering and icon choice.
      repository.ts  — Notification persistence: paging, read state and the live unread badge.
    post/
      model.ts  — The post entity: shape, defaults, derived text and edit rights.
      repository.ts  — Post persistence: the feed cursor, create, edit, save, soft delete and recovery.
    profile/
      follow.ts  — The follow graph: who follows whom, and the counts behind it.
      model.ts  — The user profile entity: shape, defaults and derived display values.
      repository.ts  — Profile persistence: read-through, write-through and live watching.
    project/
      model.ts  — The project entity: what someone is building, and who is helping.
      repository.ts  — Project persistence: publish, browse, join and leave.
    reaction/
      model.ts  — Reactions as data: one per person per post, plus the derived summary.
      repository.ts  — Reaction persistence: set, clear and watch.
    report/
      catalog.ts  — The reports BSDC issues, and the rows each one is made of.
      document.ts  — The record of an issued report, and the verdict a verifier reaches from it.
      repository.ts  — Issuing a report and checking one.
    reputation/
      model.ts  — Reputation: points, level, streak and the badges they have earned.
      repository.ts  — Reputation reads: a record, a leaderboard, and the opt-out switch.
    search/
      model.ts  — The search document: one uniform shape every kind of content is flattened into.
      ranking.ts  — The one scoring function shared by remote search and device search.
      repository.ts  — Search execution: the device index, the remote prefix queries, and their merge.
    story/
      model.ts  — The Real Story entity: a 24-hour frame of someone's day, and nothing more.
      repository.ts  — Story persistence: publish, read the live rail, record a view, expire and recover.
  features/
    admin/
      AdminScope.tsx  — The wrapper that decides whether an administration screen is shown at all.
      AuditTrail.tsx  — The trail: who did what, to whom, and what it was before.
      FlagMatrix.tsx  — The feature register: what is on, what is off, and what is off between two dates.
      PasskeyDialog.tsx  — The passkey gate that stands in front of a privileged change.
      RecoveryBin.tsx  — The recovery bin: what was deleted, what can still come back, and when it stops being
      RoleAssignment.tsx  — The form that changes somebody's standing on the platform.
      index.ts  — Public surface of the administration feature.
    auth/
      RequireAuth.tsx  — Route guard for surfaces that need an identity.
      SessionProvider.tsx  — The one React context that owns identity, claims and the profile in view.
      SignInCard.tsx  — The sign-in surface: email link, password, new account, or this device only.
      index.ts  — Public surface of the identity feature.
      session.ts  — The session model: what the app knows about the person using it.
      useSession.ts  — Hooks over the session context: identity, role checks and permission checks.
    comments/
      CommentComposer.tsx  — Writing a comment or a reply: text, counter and submit.
      CommentThread.tsx  — A post's comments: threaded one level, paged, live.
      index.ts  — Public surface of the comments feature.
    composer/
      MediaTray.tsx  — The composer's attachment tray: thumbnails, progress, alt text and failures.
      PostComposer.tsx  — The universal publisher: text, images, visibility, group and schedule.
      index.ts  — Public surface of the composer feature.
      useComposer.ts  — Composer state: body, attachments, visibility, schedule and submission.
    events/
      EventCard.tsx  — One event, in the list: when, where, and whether there is room.
      EventForm.tsx  — Creating or editing an event, including where it happens.
      EventList.tsx  — The event directory: filters, phases, and the honest empty state.
      RsvpBar.tsx  — Answering an invitation: going, interested, or not this time.
      VenuePicker.tsx  — Picking a venue on an OpenStreetMap map.
      index.ts  — Public surface of the events feature.
    feed/
      FeedStream.tsx  — The feed itself: cursor paging, infinite scroll, pull to refresh, virtualisation.
      PostCard.tsx  — One post in a stream: author, body, attachments, reactions, comments and actions.
      PostMedia.tsx  — Post attachments: blur preview, real dimensions, responsive sources, no layout shift.
      index.ts  — Public surface of the feed feature.
    freelancer/
      GigCard.tsx  — One service someone sells, with the price they actually charge.
      GigForm.tsx  — Publishing a service: what you do, and what it costs.
      GigList.tsx  — The freelancer directory, with the categories that matter to this community.
      OrderCard.tsx  — One order, and the two or three actions a person can take on it.
      OrderDialog.tsx  — Placing an order: what you need, when you need it, and what it costs.
      index.ts  — Public surface of the freelancer hub.
    gamification/
      BadgeShelf.tsx  — The badges somebody has earned, and the ones still within reach.
      Leaderboard.tsx  — The public standing of the community, one window at a time.
      LevelRing.tsx  — Where somebody stands on the ladder, and how far the next rung is.
      PointsChip.tsx  — One line saying what a person's contributions are worth here.
      StreakCard.tsx  — The daily-streak card, and the honest sentence about what a streak is worth.
      index.ts  — Public surface of the gamification feature.
    groups/
      CreateGroupDialog.tsx  — Creating a group: name, description, privacy, category and place.
      GroupCard.tsx  — One group in a list: identity, privacy, size and the join control.
      index.ts  — Public surface of the groups feature.
    jobs/
      ApplicationForm.tsx  — Applying to a job, with enough words to be worth reading.
      ApplicationList.tsx  — The employer's view of who applied, and where each one stands.
      JobCard.tsx  — One job, in the board: what it is, where it is, what it pays and when it closes.
      JobFilters.tsx  — Narrowing the board down to the jobs that fit.
      JobForm.tsx  — Posting a job that a person can actually decide about.
      index.ts  — Public surface of the jobs feature.
    messenger/
      ChatPanel.tsx  — One conversation: history, composer, receipts, typing and presence.
      ConversationList.tsx  — The conversation rail: who you are talking to, the last line, and unread counts.
      MessageBubble.tsx  — One chat message: body, attachment, time and delivery state.
      TypingIndicator.tsx  — The "someone is typing" line in a conversation.
      index.ts  — Public surface of the messenger feature.
      useUnreadConversations.ts  — The unread conversation count shown on the messenger tab.
    moderation/
      AppealForm.tsx  — Asking a different human to look again.
      ModerationQueue.tsx  — The reviewer's working screen: what is waiting, and what is past due.
      ReportCard.tsx  — One report, as a reviewer sees it: what, who, how urgent, and what can be done.
      ReportDialog.tsx  — Filing a report: what happened, and enough words for a human to act on it.
      index.ts  — Public surface of the moderation feature.
    notifications/
      BroadcastComposer.tsx  — The manual broadcast desk: an admin writes, an admin presses send.
      NotificationList.tsx  — The notification centre: what happened, who did it, and where it opens.
      PushOptIn.tsx  — Turning web push on, and saying plainly when it cannot be turned on.
      index.ts  — Public surface of the notifications feature.
      useUnreadCount.ts  — One hook that owns the unread notification count for the whole shell.
    presence/
      PresenceDot.tsx  — A person's live presence indicator, driven by the Realtime Database.
      index.ts  — Public surface of the presence feature.
    profile/
      FollowButton.tsx  — The one control that changes whether somebody's work reaches you.
      ProfileCard.tsx  — One person, in one row, wherever a list of people is needed.
      ProfileEditor.tsx  — Editing your own profile: the presentation fields, and only those.
      ProfileHeader.tsx  — The top of a profile: who the person is, and what they have done here.
      index.ts  — Public surface of the profile feature.
      useProfile.ts  — Resolves a handle or an account id into a profile, with follow state and counts.
    projects/
      ProjectCard.tsx  — One project: what it is, what it is built with, and whether it wants help.
      ProjectForm.tsx  — Starting a project, and saying what kind of help would be useful.
      ProjectList.tsx  — The project directory, filtered by status and by whether help is wanted.
      index.ts  — Public surface of the projects feature.
    reactions/
      ReactionBar.tsx  — The reaction row: summary, quick react, and the ten-glyph chooser.
      ReactionGlyph.tsx  — One reaction glyph, drawn from the published sprite.
      ReactionPicker.tsx  — The ten-reaction chooser, opened from a long press, hover or keyboard focus.
      index.ts  — Public surface of the reactions feature.
    reports/
      ReportComposer.tsx  — Generate a report: choose one, see its rows, then issue it as a verifiable PDF.
      VerificationCard.tsx  — The public checker: paste an id or a URL, learn whether the document is the one we issued.
      index.ts  — Public surface of the reports feature.
    search/
      CommandPalette.tsx  — The command palette: one keystroke to anywhere, and one place to search from.
      SearchDialog.tsx  — The dialog that hosts the command palette, and the global shortcut that opens it.
      SearchResults.tsx  — The rendered result list: one group per kind, one row per hit.
      index.ts  — Public surface of the search feature.
      recentSearches.ts  — The last few things a person searched for on this device.
      useCommandPalette.ts  — The open state of the command palette, and the shortcut that toggles it.
      useSearch.ts  — Search state for a screen: debounced query, results, provenance and loading.
    stories/
      StoryComposer.tsx  — Publishing a story: pick an image, say a sentence, and let it go in twenty-four hours.
      StoryRail.tsx  — The story rail: one ring per person, ordered by who you follow first.
      StoryViewer.tsx  — The full-frame story viewer: advance, go back, and let it end.
      index.ts  — Public surface of the Real Story feature.
  main.tsx  — Browser entry point (ADR-010).
  pages/
    about/
      AboutPage.tsx  — Who BSDC is, who owns it and what transparency commitments bind it (PART 31.4).
    admin/
      AdminAuditPage.tsx  — The audit trail screen.
      AdminFeaturesPage.tsx  — The feature register screen.
      AdminOverviewPage.tsx  — The administration home: how much is switched off, how much is about to be lost, and
      AdminRecoveryPage.tsx  — The recovery bin screen.
      AdminRolesPage.tsx  — The role assignment screen.
    design-system/
      DesignSystemPage.tsx  — The living design-system lab: tokens, themes, typography, primitives, contrast and the
      tokenGroups.ts  — The token catalogue rendered by the design-system lab (PART 08.09, 08.10).
    events/
      EventDetailPage.tsx  — One event: when, where, who is coming, and your answer.
      EventsPage.tsx  — The event directory: what is happening, and what you said you would attend.
    feed/
      FeedPage.tsx  — The feed route: compose, choose a sort, and read what the community published.
    freelancer/
      FreelancerPage.tsx  — The freelancer hub: services people sell, and the orders on both sides.
    groups/
      GroupsPage.tsx  — The groups route: discover communities, join them, or start one.
    home/
      HomePage.tsx  — The BSDC landing surface: what the platform is, what it does and when it launches.
    jobs/
      JobDetailPage.tsx  — One job, and the two different screens it becomes depending on who is reading.
      JobsPage.tsx  — The job board: what is open, filtered to what fits, and what you have applied to.
    leaderboard/
      LeaderboardPage.tsx  — The public standing of the community, and your own place in it.
    messages/
      MessagesPage.tsx  — The messenger route: conversation rail on the left, open thread on the right.
    moderation/
      ModerationPage.tsx  — The reviewer's screen, and the reporter's own track record.
    network/
      NetworkPage.tsx  — The RRC network hub page: Bangladesh's first unified open digital ecosystem
    notifications/
      NotificationsPage.tsx  — The notification centre route.
    profile/
      ProfilePage.tsx  — One member, in public: who they are, what they post, and what they build.
    projects/
      ProjectsPage.tsx  — The project directory, and the place a builder asks for help.
    reports/
      ReportsPage.tsx  — Where a staff member issues a report.
      VerifyReportPage.tsx  — The public verification page the QR code points at.
    search/
      SearchPage.tsx  — The full search route: everything the platform found, grouped by kind.
    stories/
      StoriesPage.tsx  — The story screen: what is live right now, and the one you have not posted yet.
    system/
      ErrorPage.tsx  — Application error page rendered by the router error element (PART 24.2).
      MaintenancePage.tsx  — Shown during a scheduled maintenance window (PART 03.05).
      NotFoundPage.tsx  — 404 page with real recovery paths (PART 07.01, PART 09.02).
      OfflinePage.tsx  — Offline fallback route served by the service worker (PART 16.1, PART 26).
  services/
    backend/
      callable.ts  — One typed door to Cloud Functions, used for every action the client may not perform.
      gateway.ts  — The single place that knows whether BSDC is talking to Firebase or to the device.
    firebase/
      app.ts  — Lazy, single-instance accessors for the Firebase JS SDK.
      database.ts  — Accessors for the Realtime Database, BSDC's ephemeral plane (ADR-018).
      firestore.ts  — Thin, typed helpers over the Firestore SDK: conversion, paging and timestamps.
    media/
      cloudinary.ts  — Durable media upload and delivery through Cloudinary (LAW-05).
      imgbb.ts  — Bulk, non-critical image upload through ImgBB (LAW-05).
      index.ts  — The one upload entry point: validate, choose a provider, upload, report progress.
      preview.ts  — Local previews: object URLs, blur previews and dominant colours.
      transport.ts  — One XMLHttpRequest upload loop with real progress, shared by every media provider.
      validate.ts  — Upload validation: what may be uploaded, how large, and through which provider.
    notifications/
      engine.ts  — The fan-in: grouping, deduplication and digests on top of raw notifications.
      oneSignal.ts  — The manual broadcast desk, and the guard rail that keeps it manual.
      preferences.ts  — What a person wants to be told, and through which door.
      tokens.ts  — Web-push registration through Firebase Cloud Messaging.
    offline/
      idb.ts  — A dependency-free IndexedDB wrapper with a memory fallback.
      mirror.ts  — The device mirror: a readable, ordered local copy of the data a person can see.
      outbox.ts  — The durable queue of mutations that have not reached the backend yet.
      sync.ts  — Read-through and write-through: the two primitives every repository is built from.
    realtime/
      presence.ts  — Account presence in the Realtime Database (PART 14.02).
      registry.ts  — The one registry every realtime subscription in BSDC is registered in.
      typing.ts  — Typing indicators and delivery receipts in the Realtime Database.
  shared/
    hooks/
      index.ts  — Public surface of the hooks library (ADR-004: narrow public APIs per module).
      useAnnounce.ts  — Screen-reader announcements for route and state changes (PART 04 LAW-14, F-457).
      useBreakpoint.ts  — Active breakpoint and navigation model from the single breakpoint source (ADR-009).
      useClipboard.ts  — Copy-to-clipboard with a transient "copied" state for share sheets (F-150).
      useControllableState.ts  — Controlled/uncontrolled state for every primitive (PART 08.09).
      useCountdown.ts  — Launch countdown that is safe under prerender and never mismatches hydration
      useDebounce.ts  — Debounced values for search and autosave (PART 25: debounced search at 250ms).
      useEventListener.ts  — Declarative event subscription with guaranteed cleanup (LAW-22).
      useInterval.ts  — Declarative interval that pauses when the tab is hidden (PART 25).
      useIsomorphicLayoutEffect.ts  — useLayoutEffect that is safe during server-side prerender (PART 10.02).
      useLocalStorageState.ts  — Persistent React state backed by the guarded storage wrapper (PART 26).
      useLockScroll.ts  — Body scroll lock for modals and sheets, without the iOS scroll-jump (PART 08.04 R-17).
      useMediaQuery.ts  — Reactive media query subscription (PART 08.04).
      useMounted.ts  — Mounted flag used to gate browser-only rendering after prerender (PART 10.02).
      useOnline.ts  — Connectivity state with a deliberate "unknown until proven" start (PART 24.2).
      usePrefersReducedMotion.ts  — Reactive reduced-motion preference (PART 08.03, LAW-14).
      useResizeObserver.ts  — Element-size observation for container-query behaviour in JS (PART 08.04 R-05).
      useVisibility.ts  — Document visibility for pausing realtime listeners and timers (PART 25).
    lib/
      clipboard.ts  — Clipboard and Web Share helpers used by the share sheet (F-148, F-149).
      cn.ts  — The single className composer used by every component in the product.
      color.ts  — Colour maths for contrast checking, avatar fallback tints and theme validation.
      date.ts  — Date and time formatting with Bangla and English first-class (PART 09.01).
      device.ts  — Device, capability and network-quality detection used to degrade gracefully
      hash.ts  — WebCrypto hashing and HMAC helpers for integrity checks, dedupe and signing
      idle.ts  — Runs work after the browser has painted the frame the person is waiting for.
      motion.ts  — Shared Framer Motion variants so every surface animates with one vocabulary
      number.bn.ts  — Bengali numeral conversion and locale number formatting (PART 09.01).
      perf.ts  — Performance measurement helpers for the ranking pipeline budgets and route timing
      relativeTime.ts  — Coarse relative-time buckets used by counters, receipts and presence labels.
      retry.ts  — Retry with exponential backoff, jitter and cancellation (PART 24.4).
      slugify.ts  — URL slugs for posts, profiles, stores, tags and products (PART 09.01, PART 10.06).
      storage.ts  — Safe, quota-aware wrappers around localStorage/sessionStorage (PART 26).
      text.ts  — Bilingual text primitives: script detection, truncation, initials and Bangla-aware
      uid.ts  — Identifier generation: short codes, referral codes, order ids, invoice numbers.
      url.ts  — Canonical URL construction, tracking-param stripping and locale path handling
      validators.ts  — Small, dependency-free validators used before a Zod schema or a Firestore rule runs,
    stores/
      appearance.ts  — Global appearance state: theme, density, font scale, numerals, motion, transparency
      badges.ts  — Shell badge counts, shared by the header, the bottom bar and the rail.
    ui/
      Accordion.tsx  — Collapsible sections for FAQ, filters and settings groups (PART 08.09).
      Avatar.tsx  — Avatar with initials fallback, presence dot, role ring and group layout
      Badge.tsx  — Status, count and role badges. Role is conveyed by colour plus text, never emoji
      BottomNav.tsx  — Bottom tab navigation for viewports at or below 768px (PART 08.04 R-04, PART 08.05).
      BrandLogo.tsx  — The BSDC brand marks: horizontal lockup, stacked lockup, shield and the app icon
      Button.tsx  — The platform button: 12 variants, 4 sizes, 3 tones, with loading and full-width modes
      Card.tsx  — Six card variants with header, body and footer slots (PART 08.09).
      Checkbox.tsx  — Checkbox with label, indeterminate state and bulk-selection support (PART 08.09).
      Chip.tsx  — Filter chips, tag chips and removable chips (PART 08.09, F-175).
      Container.tsx  — Layout primitives: page container, responsive container, stack and auto-grid
      Dialog.tsx  — Confirmation before destructive actions, optionally requiring a typed phrase
      Drawer.tsx  — Navigation drawer for the mobile shell and the admin panel (PART 08.09).
      DropdownMenu.tsx  — Action menus for posts, comments, chats and admin tables (PART 08.09).
      EmptyState.tsx  — Empty and error states: illustration, one-line explanation, one primary action
      Icon.tsx  — The component icon registry. Icons are SVG only — the product ships no emoji (LAW-01).
      IconButton.tsx  — Square icon-only button. Always labelled for assistive technology (LAW-14).
      InfiniteScrollSentinel.tsx  — Cursor-pagination sentinel with end-of-list and error states (F-165).
      Input.tsx  — Field, input, textarea and input group with label, hint, error and counter
      Kbd.tsx  — Keyboard-shortcut hint (PART 08.09, F-214).
      LazyDropdownMenu.tsx  — A dropdown whose menu implementation is loaded on first interaction (PART 25).
      Modal.tsx  — Centred dialog for pointer-fine viewports; renders as a sheet on touch (PART 08.04 R-19).
      Popover.tsx  — Inline panels for filters, pickers and explainers (PART 08.09).
      ProgressRing.tsx  — Circular progress for levels, quotas and quotas (F-302, F-508) plus linear progress.
      PullToRefresh.tsx  — Pull-to-refresh for feeds, notifications, chats, orders and dashboards
      RadioGroup.tsx  — Radio groups and selectable cards for plan and post-type pickers (PART 08.09).
      RailNav.tsx  — Left rail navigation between 1024px and 1679px (PART 08.04 R-04).
      SafeArea.tsx  — Safe-area aware wrapper for notched phones and Android gesture bars
      ScrollArea.tsx  — Scroll container with contained overscroll (PART 08.09).
      Select.tsx  — Accessible select with a scroll-contained viewport (PART 08.09).
      Separator.tsx  — Horizontal and vertical dividers, with an optional label (PART 08.09).
      Sheet.tsx  — Bottom sheet on touch, side sheet with a pointer (PART 08.04 R-19, PART 08.05).
      Skeleton.tsx  — Loading placeholders that reserve the final layout (F-168, CLS budget).
      Slider.tsx  — Single and range sliders for discovery preferences and volume (F-180, PART 08.09).
      Spinner.tsx  — Inline loading indicator with an accessible label (PART 08.09).
      StickyHeader.tsx  — Header that condenses on scroll and never eats more than 15% of the viewport
      Switch.tsx  — Switch and switch row for settings and feature flags (PART 08.09).
      Tabs.tsx  — Accessible tabs with a horizontally scrollable rail (PART 08.09, F-164).
      Tooltip.tsx  — Supplementary tooltips, suppressed on touch where hovering does not exist
      Typography.tsx  — Text and heading primitives with the fluid type scale (PART 08.02).
      VirtualList.tsx  — Windowed list for anything over 100 rows (PART 04 LAW-20, PART 25).
      index.ts  — Public surface of the design-system primitives (ADR-004: narrow public APIs).
      toast.ts  — Typed toast helpers over the Sonner host (PART 08.09, F-119).
  styles/
    components/
      accordion.css  — Accordion for FAQ, settings groups and admin filters (PART 08.09).
      admin.css  — The administration console: panels, the flag register, the audit table, the bin.
      avatar.css  — Avatar, avatar group and presence ring (PART 08.09, F-029).
      badge.css  — Status, count and role badges. Role is encoded by colour plus label text plus an SVG
      button.css  — Button appearance: 12 variants, 4 sizes, 3 tones (PART 08.09).
      card.css  — Six card variants used by every content surface (PART 08.09).
      checkbox.css  — Checkbox and indeterminate state (PART 08.09).
      chip.css  — Filter chips, tag chips and removable chips (PART 08.09, F-175).
      comments.css  — Comment threads, one level deep.
      composer.css  — Composer shell, toolbar, dropzone and preview panel (PART 08.09).
      dialog.css  — Alert dialog styling — the confirm-before-destructive surface (LAW-12, LAW-19).
      drawer.css  — Navigation drawer used by the mobile shell and the admin panel (PART 08.09).
      dropdown.css  — Menu, submenu, item, separator and shortcut styling (PART 08.09).
      empty-state.css  — Empty and error states — illustration, one-line explanation, one primary action
      feed.css  — Feed column, item separation and the new-posts pill (F-166).
      groups.css  — Group cards and the group grid.
      input.css  — Field, textarea, group and helper-text styling (PART 08.09).
      messenger.css  — Messenger: conversation rail, thread, bubbles, composer and typing line.
      modal.css  — Centred dialog surface for pointer-fine viewports (PART 08.04 R-19).
      nav.css  — Bottom navigation, left rail, right rail and header geometry (PART 08.04 R-04, R-18).
      notifications.css  — The notification centre.
      opportunities.css  — Styling for search, profile, stories, events, jobs, projects, the freelancer hub,
      popover.css  — Popover surface used by filters, pickers and inline help (PART 08.09).
      post.css  — The post card: header, body, media grid and action row.
      presence.css  — Presence indicator and the account menu.
      progress.css  — Linear progress, circular progress ring and indeterminate state (PART 08.09).
      radio.css  — Radio group items and cards (PART 08.09).
      reactions.css  — Reaction row, chooser and glyph tints.
      scroll-area.css  — Scroll container with contained overscroll and thin scrollbars (PART 08.09).
      select.css  — Select trigger, viewport and option styling (PART 08.09).
      separator.css  — Horizontal and vertical dividers (PART 08.09).
      sheet.css  — Bottom sheet for touch viewports and side sheet for pointer-fine (PART 08.04 R-19).
      skeleton.css  — Loading placeholders that match the final layout exactly (F-168, CLS budget).
      slider.css  — Slider used by discovery preferences and volume control (F-180).
      switch.css  — Switch control for settings and feature-flag toggles (PART 08.09).
      table.css  — Data tables for admin, vendor and report surfaces (PART 08.04 R-20, PART 05.03 note).
      tabs.css  — Tab list, trigger and panel styling for feed tabs and settings (PART 08.09).
      toast.css  — Toast content styling for the Sonner host (PART 08.09).
      tooltip.css  — Tooltip surface (PART 08.09).
      virtual-list.css  — Windowed list container (PART 04 LAW-20, PART 25).
    index.css  — Style entry point. Imports every layer in a single, documented order.
    layers/
      base.css  — Element defaults for the BSDC shell: canvas, focus behaviour, skip link, scrollbars
      print.css  — Print stylesheet for A4 documents and 4x6 thermal labels (PART 08.04 R-14).
      reset.css  — A modern, minimal reset. Tailwind preflight is disabled (ADR-008) because the
      themes.css  — The nine switchable themes plus density modes (PART 08.01).
      tokens.css  — The complete design token layer: colour, spacing, typography, radius, elevation,
      typography.css  — Type scale application, prose rhythm, numeral systems and code typography
      utilities.css  — Layout utilities that Tailwind cannot express cleanly: safe-area padding, logical
    responsive/
      dark.css  — Cross-cutting dark-theme corrections that are not pure token swaps.
      desktop.css  — 1280px to 1679px — the classic three-region desktop layout.
      foldable.css  — Foldables and dual-screen devices (PART 08.04 R-10).
      forced-colors.css  — Windows High Contrast / forced-colors mode support (LAW-14).
      high-contrast.css  — System-level and user-selected high contrast support (PART 08.01 theme 7, LAW-14).
      hover.css  — Hover affordances only where hovering exists (PART 08.04 R-15).
      laptop.css  — 1024px to 1279px — the rail-navigation shell (PART 08.04 R-04).
      large.css  — 1680px to 2559px — the three-column shell with a wider feed and chat dock.
      mobile-250.css  — The 250px contract — the primary design target, not an afterthought (PART 08.04).
      mobile-small.css  — 320px to 359px — small Android phones and embedded webviews.
      mobile.css  — 360px to 767px — the default phone experience (PART 08.05 "feels like a mobile app").
      oled.css  — OLED pure-black refinements (PART 08.01 theme 3).
      orientation.css  — Orientation-specific rules (PART 08.04 R-09).
      pointer.css  — Pointer precision rules and tap-target sizing (PART 08.04 R-03).
      reduced-motion.css  — Honour prefers-reduced-motion across the whole product (PART 08.03, PART 08.07).
      tablet.css  — 768px to 1023px — tablets, small laptop windows and landscape phones.
      tv-4k.css  — 2560px and above — large monitors and 50-inch class televisions (PART 08.04 R-06).
      ultra-wide.css  — 3440px ultra-wide and 5120px 5K displays.
    vendors/
      radix-overrides.css  — Maps Radix primitive state onto BSDC tokens so no Radix default colour ever leaks
      tailwind.css  — Tailwind entry point. Preflight is disabled in tailwind.config.ts because
  tests/
    components/
      opportunityRoutes.test.tsx  — Smoke test of the opportunity routes: every new screen mounts, in Bangla, without a
      primitives.test.tsx  — Component coverage for the design-system primitives (PART 23.4, PART 08.09).
      reactionBar.test.tsx  — Proves the reaction row is tappable, announces itself, and reacts optimistically.
      shell.test.tsx  — Smoke test of the application shell: providers, router, translations and the first
      signIn.test.tsx  — Proves the sign-in surface offers every real path and that a device session is a real
    helpers/
      i18n.ts  — Feeds i18next from the real dictionaries on disk inside component tests.
    setup.ts  — jsdom environment setup for the component suite (PART 23.4).
    unit/
      backendGateway.test.ts  — Proves the gateway degrades instead of breaking (ADR-019).
      commentModel.test.ts  — Proves comment construction and one-level threading.
      conversationModel.test.ts  — Proves conversation identity, message construction and read-receipt maths.
      conversationThread.test.ts  — Proves message reactions, edit windows, delivery state and reply threading.
      eventModel.test.ts  — Proves event validation, phase, seat maths and RSVP waiting-list behaviour.
      flags.test.ts  — Unit coverage for the feature-flag client, including scheduled windows (LAW-11).
      gigModel.test.ts  — Proves the freelancer hub's money and time rules.
      groupRequests.test.ts  — Proves join requests, group roles and the secret-group visibility rule.
      jobModel.test.ts  — Proves the job board's honesty rules: salary, deadlines and applications.
      limits.test.ts  — Unit coverage for the platform limit registry and validators (PART 11.03).
      listenerRegistry.test.ts  — Proves every realtime subscription is reference-counted and always released.
      mediaValidate.test.ts  — Proves the upload gate: no video, correct ceilings, correct provider per surface.
      moderationState.test.ts  — Proves the report state machine, the appeal rules and the SLA maths.
      notifications.test.ts  — Proves notification preferences, grouping, urgency and the manual-broadcast law.
      number.bn.test.ts  — Unit coverage for Bengali numeral handling and BDT formatting (PART 09.01).
      offline.test.ts  — Proves the device mirror and the outbox keep their promises.
      pathRegistry.test.ts  — Proves the Firestore and Realtime Database path builders produce the exact paths the
      permissions.test.ts  — Proves the entitlement matrix behaves as documented (PART 05.02, LAW-03).
      postModel.test.ts  — Proves post construction, scheduling, visibility and edit rights.
      reactions.test.ts  — Proves reaction counting, summary derivation and optimistic updates are exact.
      reportIdentity.test.ts  — Proves that a report can be checked: ids are well-formed, hashes are real, the same
      reputationModel.test.ts  — Proves the level curve, the streak rules and the leaderboard ranking.
      routeTable.test.ts  — Proves the route table is consistent: every live route is translatable, indexable
      searchRanking.test.ts  — Proves that search answers the question a person asked, in both scripts.
      sessionModel.test.ts  — Proves the session model: sources, roles and the publishing rule.
      storyProjectModel.test.ts  — Proves that stories disappear on time, and that projects say honestly whether they
      text.test.ts  — Unit coverage for the bilingual text primitives (PART 23.4).
      url.test.ts  — Unit coverage for canonical URL handling (PART 10.06).
  vite-env.d.ts  — Typed contract for `import.meta.env` (PART 06.01).
  widgets/
    countdown/
      LaunchCountdown.tsx  — Launch countdown driven by admin settings, safe under prerender (PART 03.05).
    footer/
      NetworkHub.tsx  — The RRC network hub card: Bangladesh's first unified open digital ecosystem
      SiteFooter.tsx  — Global footer: RRC network hub, legal line, language and status (PART 03.02).
    header/
      AccountMenu.tsx  — The header's account control: who you are, your unread count, and how to sign out.
      LocaleSwitcher.tsx  — Bangla and English switcher, both first-class (PART 09.01).
      ThemeToggle.tsx  — Theme switcher for the nine themes plus density and font scale (PART 08.01, 08.08).
      TopBar.tsx  — The application header: brand, search entry, theme, language and account actions
    maintenance-banner/
      MaintenanceBanner.tsx  — Announces a scheduled maintenance window (PART 03.05 scheduled windows, F-476).
    mobile-nav/
      BottomNavWidget.tsx  — Shell-level bottom navigation with live route filtering and unread counts
    shell/
      BadgeSync.tsx  — Populates the shell badge counts. Rendered lazily, after the first paint.
      CommandPaletteHost.tsx  — The one place the command palette lives, and the one place its shortcut is registered.
storage.rules
tailwind.config.ts  — Maps every Tailwind utility onto the BSDC design tokens (CSS custom properties)
tools/
  generate-icons.mjs  — Generates every raster icon and share image from the SVG masters (PART 28.2).
  generate-messaging-config.mjs  — Writes the public Firebase configuration the messaging service worker imports.
  generate-sprite.mjs  — Rebuilds the reaction section of the UI sprite from the SVG masters and publishes it.
  lint/
    eslint-plugin-bsdc.mjs  — Local ESLint plugin holding the two platform laws that no off-the-shelf rule covers:
    no-emoji.mjs  — Standalone LAW-01 scanner. Covers everything ESLint cannot parse: Markdown, JSON,
tsconfig.app.json
tsconfig.json
tsconfig.node.json
vite.config.ts  — Build and dev-server configuration for the BSDC application shell.
vitest.config.ts  — Test runner configuration for unit and component suites.
```

Files described: 598.
